import React from 'react';

const Inventory = () => {
    const style = {
        width: '100%',
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    }
    return (
        <div style={style}>
            <h1>Developer is sleeping</h1>
        </div>
    );
};

export default Inventory;